diff --git a/.env.example b/.env.example
index 4e6ace9..755ab11 100644
--- a/.env.example
+++ b/.env.example
@@ -1 +1,2 @@
+# тут название одно а используется другое
 NEXT_PUBLIC_BACK_URL=
\ No newline at end of file
diff --git a/src/actions/auth.ts b/src/actions/auth.ts
index e5dd349..c4bef43 100644
--- a/src/actions/auth.ts
+++ b/src/actions/auth.ts
@@ -133,6 +133,7 @@ export async function registerAction(
   const parsed = RegisterSchema.safeParse(rawData);
 
   if (!parsed.success) {
+    // как пользователю что ему надо исправить?
     return { error: 'Validation error on server' };
   }
 
@@ -144,6 +145,7 @@ export async function registerAction(
   });
 
   if (!res.ok) {
+    // это не информативная ошибка
     return { error: 'Backend error' };
   }
 
diff --git a/src/app/(authorize-users)/(user-options)/account/page.tsx b/src/app/(authorize-users)/(user-options)/account/page.tsx
index e88bee4..0bbb9c7 100644
--- a/src/app/(authorize-users)/(user-options)/account/page.tsx
+++ b/src/app/(authorize-users)/(user-options)/account/page.tsx
@@ -1,5 +1,7 @@
+// названия компонентов должны быть в PascalCase (поправь везде)
 const account = () => {
   return (
+    // тут нужен fragment? (в результатирующем коде икак не мешает. Он нужен только когда возвращаем более одного элемента)
     <>
       <div className="pt-52 text-center">Welcome in account page</div>
     </>
diff --git a/src/app/layout.tsx b/src/app/layout.tsx
index 5a00951..4b4c9bb 100644
--- a/src/app/layout.tsx
+++ b/src/app/layout.tsx
@@ -16,6 +16,9 @@ export const metadata: Metadata = {
   description: 'Generated by create next app',
 };
 
+// у тебя в components есть pages что выглядт странно. подбери пожалуйста более подходящее название. Можешь
+// ссылаться на FSD
+
 export default async function RootLayout({
   children,
 }: Readonly<{
diff --git a/src/components/Header.tsx b/src/components/Header.tsx
index a04c569..ef403d6 100644
--- a/src/components/Header.tsx
+++ b/src/components/Header.tsx
@@ -56,7 +56,7 @@ export default function Header({ user }: HeaderProps) {
               </Button>
             </>
           )}
-
+          {/*  у тебя есть кнопка переводов но нет переводов */}
           <div className="flex items-center gap-1 cursor-pointer hover:opacity-80">
             <IoLanguageSharp className="w-6 h-6" />
             <span>EN</span>
diff --git a/src/components/pages/AuthForm.tsx b/src/components/pages/AuthForm.tsx
index 9ee1714..abb7df4 100644
--- a/src/components/pages/AuthForm.tsx
+++ b/src/components/pages/AuthForm.tsx
@@ -30,6 +30,7 @@ function AuthForm() {
               error={state.fieldErrors?.[name]?.[0]}
               disabled={isPending}
             />
+            // тут отображается ошибка валидации данного поля
           ))}
 
           {state.error && (
diff --git a/src/components/pages/RegistrationForm.tsx b/src/components/pages/RegistrationForm.tsx
index 0a42f56..80aa038 100644
--- a/src/components/pages/RegistrationForm.tsx
+++ b/src/components/pages/RegistrationForm.tsx
@@ -26,6 +26,7 @@ const RegistrationForm = () => {
               disabled={isPending}
               error={state.fieldErrors?.[name]?.[0]}
             />
+            // tут отображается ошибка валидации данного поля
           ))}
 
           {state.error && <p className="text-sm text-red-500">{state.error}</p>}
diff --git a/src/modules/Input.tsx b/src/modules/Input.tsx
index 9d7afcc..346ecfa 100644
--- a/src/modules/Input.tsx
+++ b/src/modules/Input.tsx
@@ -4,6 +4,7 @@ interface IInputProps extends InputHTMLAttributes<HTMLInputElement> {
   error?: string;
 }
 
+// в react 19 ref это обычный проп
 export const Input = forwardRef<HTMLInputElement, IInputProps>(
   ({ name, placeholder, type, error, ...props }, ref) => {
     return (
diff --git a/src/proxy.ts b/src/proxy.ts
index 18eb7d4..f56113c 100644
--- a/src/proxy.ts
+++ b/src/proxy.ts
@@ -7,8 +7,10 @@ export function proxy(request: NextRequest) {
 
   const publicRoutes = ['/login', '/registration'];
 
+  // тут надо будет заменить название на isOnlyPublicRoute потому как у тебя будут и просто публичные роуты
   const isProtectedRoute = publicRoutes.includes(currentPath);
-
+  // не авторизованный должен не попадать на защищенные страницы
+  // когда добавишь приватные маршруты то тут надо будет поправить
   if (!session && !isProtectedRoute) {
     return NextResponse.redirect(new URL('/login', request.url));
   }
diff --git a/src/utils/constants.ts b/src/utils/constants.ts
index 84f541a..bc9138c 100644
--- a/src/utils/constants.ts
+++ b/src/utils/constants.ts
@@ -1,5 +1,6 @@
 import { InputAuth, InputRegister } from './types';
-
+// эти переменные используются 1 раз. Ты планируешь их использовать где-то еще?
+// если нет то они должны быть там где используются а не на глобальном уровне
 export const inputsTypeAuth: InputAuth[] = [
   { name: 'username', placeholder: 'User Name', type: 'text' },
   { name: 'password', placeholder: 'Password', type: 'password' },
diff --git a/src/utils/types.ts b/src/utils/types.ts
index 9db9883..e53976d 100644
--- a/src/utils/types.ts
+++ b/src/utils/types.ts
@@ -1,5 +1,5 @@
 import z from 'zod';
-
+// почему сущности относящмеся только к авторизации находятся на глобальном уровне?
 export const AuthSchema = z.object({
   username: z.string().min(5, 'Minimum 5 symbols').max(40, 'Maximum 40 symbols'),
   password: z.string().min(6, 'Minimum 6 symbols').max(40, 'Maximum 40 symbols'),
